import 'package:flutter/material.dart';

class ApproveManual extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          'คู่มือการใช้งาน',
        ),
      ),
    );
  }
}
